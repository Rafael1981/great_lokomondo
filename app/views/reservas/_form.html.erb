<%= form_with(model: reserva) do |form| %>
  <% if reserva.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reserva.errors.count, "error") %> prohibited this reserva from being saved:</h2>

      <ul>
        <% reserva.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data_reserva, style: "display: block" %>
    <%= form.datetime_field :data_reserva %>
  </div>

  <div>
    <%= form.label :data_devolucao, style: "display: block" %>
    <%= form.datetime_field :data_devolucao %>
  </div>

  <div>
    <%= form.label :status_reserva, style: "display: block" %>
    <%= form.text_field :status_reserva %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
